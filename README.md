# TP02-PL_a18586_a20750

## Instructions

To start the configuration, it is necessary to install `python.exe` and add the path in environment variables.

Install the following libraries:
```sh
pip install virtualenv
pip install ply
pip install py-yacc

Select a name for your folder:
virtualenv 'name_u_folder'

Activate the virtual environment:
.\'name_u_folder'\Scripts\activate

How to Use:
To execute the interpreter, follow these steps:
Open the terminal.
Navigate to the directory where the main.py file is located.

Execute the following command:
py main.py entrada.fca

In the terminal, you will be prompted to enter the commands as shown below:

Enter the necessary commands as indicated:
tmp_01 := 2*3+4 ;
a1_ := 12345 - (5191 * 15) ;
idade_valida? := 1;
mult_3! := a1_ * 3 ;
PRINT(mult_3!);
After entering the commands, press Ctrl+Z (or Ctrl+D on Linux/macOS), and then press Enter.

Wait for the commands to execute. The result will be displayed on the terminal.

Supported Commands
Assignment to a variable: Variable identifiers must start with a lowercase letter or underscore (_). In the following positions, they can have any combination of letters, digits, and underscores. The variable name can end with a question mark (?) or an exclamation mark (!).

Arithmetic expressions: Arithmetic expressions can be assigned to a variable.

Reading values: Values can be read from user input.

Random value generation: Random values can be generated.

Output: The result can be printed to the screen.

Requirements
Python 3.x
Contributing Skunkkzen and afas0

Let me know if there's anything else you need!






